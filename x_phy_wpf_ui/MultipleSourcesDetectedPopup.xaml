<Window x:Class="x_phy_wpf_ui.MultipleSourcesDetectedPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="X-PHY"
        Width="380" SizeToContent="Height"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowActivated="False"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Window.Resources>
        <Style x:Key="PrimaryActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1D4ED8"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Border Background="White" CornerRadius="12" Padding="0,0,0,14" BorderThickness="0" BorderBrush="#D0D0D0" ClipToBounds="True">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.2"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header: logo, title + version, close -->
            <Border Grid.Row="0" Background="#E8F4FC" CornerRadius="10,10,0,0" Padding="12,10,10,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Image Grid.Column="0" Source="/x-phy-inverted-logo.png" Height="24" Stretch="Uniform" VerticalAlignment="Top" Margin="0,2,8,0" RenderOptions.BitmapScalingMode="HighQuality"/>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="0,0,8,0">
                        <TextBlock Text="Start Deepfake Detection?"
                                   FontWeight="Bold" FontSize="14" Foreground="#1A1A1A"
                                   TextWrapping="Wrap"/>
                        <TextBlock x:Name="VersionText" Text="Version: "
                                   Foreground="#666666" FontSize="11" Margin="0,2,0,0"/>
                    </StackPanel>
                    <Button Grid.Column="2" Content="✕" Width="28" Height="28"
                            Background="Transparent" Foreground="#333333" FontSize="14"
                            Cursor="Hand" BorderThickness="0" Click="CloseButton_Click"
                            VerticalAlignment="Top" Padding="0">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="4">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#D0E8F4"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <!-- Subtitle, message, DO NOT DISTURB + OPEN -->
            <StackPanel Grid.Row="1" Margin="20,16,20,16">
                <TextBlock Text="Multiple Media Source Detected"
                           FontSize="13" Foreground="#1A1A1A" Margin="0,0,0,6"/>
                <TextBlock Text="Open The App To Choose One For Detection."
                           FontSize="13" Foreground="#333333" Margin="0,0,0,16"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="DO NOT DISTURB"
                            Style="{StaticResource PrimaryActionButtonStyle}"
                            ToolTip="Mute source alerts for this session"
                            Click="DoNotDisturbButton_Click" HorizontalAlignment="Stretch"/>
                    <Button Grid.Column="2" Content="OPEN"
                            Style="{StaticResource PrimaryActionButtonStyle}"
                            Click="OpenApplication_Click" HorizontalAlignment="Stretch"/>
                </Grid>
            </StackPanel>
        </Grid>
    </Border>
</Window>
