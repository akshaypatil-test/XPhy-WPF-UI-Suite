<UserControl x:Class="x_phy_wpf_ui.Controls.LoaderComponent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="Transparent">
    <UserControl.Resources>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#B0B0B0"/>
        <SolidColorBrush x:Key="PrimaryPink" Color="#E2156B"/>
        <SolidColorBrush x:Key="RollingArcStroke" Color="#E97669"/>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left side: loader content -->
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Height="277">

            <!-- Xphy lock icon with glow and rolling-arc loader (arc drawn around icon) -->
            <Grid Width="120" Height="120" Margin="0,0,0,32" HorizontalAlignment="Center">
                <!-- Rolling arc: same center as icon, radius just outside 72x72 icon -->
                <Path x:Name="RollingArcPath" Fill="Transparent" Width="120" Height="120" StrokeThickness="4"
                      StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                      Stretch="None" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Path.Stroke>
                        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                            <GradientStop Color="#FF2D0314" Offset="0"/>
                            <GradientStop Color="#FFE2156B" Offset="1"/>
                        </LinearGradientBrush>
                    </Path.Stroke>
                </Path>
                <!-- Xphy lock icon with glow (centered in 120x120) -->
                <Grid Width="72" Height="72" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Grid.Effect>
                        <DropShadowEffect Color="#FF1AB4CC" BlurRadius="30" ShadowDepth="0" Opacity="0.6"/>
                    </Grid.Effect>
                    <Image Source="/xphy_lock_icon.png" Stretch="Uniform"/>
                </Grid>
            </Grid>

            <!-- Pink X-PHY logo -->
            <Image Source="/x-phy.png" Width="83" Height="48" Stretch="Uniform" HorizontalAlignment="Center" Margin="0,0,0,24">
                <Image.Effect>
                    <DropShadowEffect Color="#E2156B" BlurRadius="0" ShadowDepth="0" Opacity="0.8"/>
                </Image.Effect>
            </Image>

            <TextBlock Text="Initializing In Progress." 
                       Foreground="{StaticResource TextPrimary}" FontSize="16" HorizontalAlignment="Center" Margin="0,0,0,8"/>
            <TextBlock Text="Please Wait..." 
                       Foreground="{StaticResource TextSecondary}" FontSize="14" HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Right side: background shows through (face/tech visual is in AuthHostView background) -->
        <Grid Grid.Column="1" Background="Transparent"/>
    </Grid>
</UserControl>
