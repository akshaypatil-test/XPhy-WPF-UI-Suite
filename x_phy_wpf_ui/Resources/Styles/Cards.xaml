<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <!-- Merge Dependencies -->
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../Brushes.xaml"/>
        <ResourceDictionary Source="../Spacing.xaml"/>
        <ResourceDictionary Source="../Radius.xaml"/>
    </ResourceDictionary.MergedDictionaries>
    
    <!-- Standard Card Border -->
    <Style x:Key="Card.Standard" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource Brush.Surface}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="{StaticResource Radius.M}"/>
        <Setter Property="Padding" Value="{StaticResource Padding.Card}"/>
    </Style>
    
    <!-- Transparent Card -->
    <Style x:Key="Card.Transparent" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource Brush.CardTransparent}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="CornerRadius" Value="{StaticResource Radius.M}"/>
        <Setter Property="Padding" Value="{StaticResource Padding.Card}"/>
    </Style>
    
    <!-- Surface Panel -->
    <Style x:Key="Panel.Surface" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource Brush.Surface}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Radius.M}"/>
    </Style>
    
    <!-- Hover Card -->
    <Style x:Key="Card.Hover" TargetType="Border" BasedOn="{StaticResource Card.Standard}">
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource Brush.SurfaceHover}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    
</ResourceDictionary>
