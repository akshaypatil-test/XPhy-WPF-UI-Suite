<Window x:Class="x_phy_wpf_ui.MultipleSourcesDetectedPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="X-PHY"
        Width="380" SizeToContent="Height"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Border Background="White" CornerRadius="12" Padding="0,0,0,14" BorderThickness="0" BorderBrush="#D0D0D0" ClipToBounds="True">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.2"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header: logo, version, close -->
            <Border Grid.Row="0" Background="#E8F4FC" CornerRadius="10,10,0,0" Padding="12,8,10,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Image Grid.Column="0" Source="/x-phy-inverted-logo.png" Height="24" Stretch="Uniform" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock x:Name="VersionText" Grid.Column="1" Text="Version: 1.0.10"
                               Foreground="#666666" FontSize="11" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                    <Button Grid.Column="2" Content="✕" Width="24" Height="24"
                            Background="Transparent" Foreground="#333333" FontSize="14"
                            Cursor="Hand" BorderThickness="0" Click="CloseButton_Click"
                            VerticalAlignment="Center" Padding="0">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="4">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#D0E8F4"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <!-- Content: title, message, Open Application button -->
            <StackPanel Grid.Row="1" Margin="20,16,20,20">
                <TextBlock Text="Multiple Media Source Detected"
                           FontWeight="Bold" FontSize="15" Foreground="#1A1A1A" Margin="0,0,0,6"/>
                <TextBlock Text="Open The App To Choose One For Detection."
                           FontSize="13" Foreground="#333333" Margin="0,0,0,16"/>
                <Button Content="Open Application"
                        Background="#2563EB" Foreground="White" FontSize="13" FontWeight="SemiBold"
                        Padding="20,12" Cursor="Hand" BorderThickness="0" HorizontalAlignment="Stretch"
                        Click="OpenApplication_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#1D4ED8"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
