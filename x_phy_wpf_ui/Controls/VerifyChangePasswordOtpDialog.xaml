<UserControl x:Class="x_phy_wpf_ui.Controls.VerifyChangePasswordOtpDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="320" d:DesignWidth="400"
             FocusVisualStyle="{x:Null}">
    <UserControl.Resources>
        <Style x:Key="OtpBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource Brush.InputBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource Brush.TextPrimary}"/>
            <Setter Property="CaretBrush" Value="{DynamicResource Brush.White}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="MaxLength" Value="1"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Width" Value="44"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PinkBtnStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource Brush.Primary}"/>
            <Setter Property="Foreground" Value="{DynamicResource Brush.White}"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="24,14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource Brush.PrimaryHover}"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="{DynamicResource Brush.Disabled}"/>
                    <Setter Property="Foreground" Value="{DynamicResource Brush.TextSecondary}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <StackPanel>
        <!-- Header: title + Close button -->
        <Grid Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Verify Your Password Change" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource Brush.TextPrimary}"
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <Button Grid.Column="1" x:Name="CloseButton" Width="32" Height="32" Padding="0"
                    VerticalAlignment="Center" HorizontalAlignment="Right"
                    Background="Transparent" BorderThickness="0" Cursor="Hand" Click="Close_Click"
                    ToolTip="Close">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Padding="4" CornerRadius="4">
                            <Viewbox Width="18" Height="18">
                                <TextBlock Text="✕" FontSize="16" Foreground="{DynamicResource Brush.TextSecondary}"/>
                            </Viewbox>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource Brush.SurfaceHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
        <TextBlock Text="Verification code" FontSize="14" Foreground="{DynamicResource Brush.TextSecondary}" Margin="0,0,0,8"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,8">
            <TextBox x:Name="Otp0" Style="{StaticResource OtpBoxStyle}" Margin="0,0,6,0"
                     TextChanged="Otp_TextChanged" PreviewKeyDown="Otp_PreviewKeyDown"/>
            <TextBox x:Name="Otp1" Style="{StaticResource OtpBoxStyle}" Margin="0,0,6,0"
                     TextChanged="Otp_TextChanged" PreviewKeyDown="Otp_PreviewKeyDown"/>
            <TextBox x:Name="Otp2" Style="{StaticResource OtpBoxStyle}" Margin="0,0,6,0"
                     TextChanged="Otp_TextChanged" PreviewKeyDown="Otp_PreviewKeyDown"/>
            <TextBox x:Name="Otp3" Style="{StaticResource OtpBoxStyle}" Margin="0,0,6,0"
                     TextChanged="Otp_TextChanged" PreviewKeyDown="Otp_PreviewKeyDown"/>
            <TextBox x:Name="Otp4" Style="{StaticResource OtpBoxStyle}" Margin="0,0,6,0"
                     TextChanged="Otp_TextChanged" PreviewKeyDown="Otp_PreviewKeyDown"/>
            <TextBox x:Name="Otp5" Style="{StaticResource OtpBoxStyle}"
                     TextChanged="Otp_TextChanged" PreviewKeyDown="Otp_PreviewKeyDown"/>
        </StackPanel>
        <TextBlock Text="Enter the code sent to your email." FontSize="13" Foreground="{DynamicResource Brush.TextSecondary}"
                   HorizontalAlignment="Center" Margin="0,0,0,16"/>
        <TextBlock x:Name="ErrorText" Text="" FontSize="12" Foreground="{DynamicResource Brush.Error}"
                   HorizontalAlignment="Center" Margin="0,0,0,12" Visibility="Collapsed"/>
        <Button x:Name="VerifyButton" Content="Verify" Style="{StaticResource PinkBtnStyle}"
                HorizontalAlignment="Center" Click="Verify_Click" IsEnabled="False" Margin="0,0,0,20"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Text="Didn't receive the code? " FontSize="13" Foreground="{DynamicResource Brush.TextSecondary}"/>
            <TextBlock Text="Resend code" FontSize="13" Foreground="{DynamicResource Brush.Primary}" Cursor="Hand"
                       MouseDown="Resend_MouseDown"/>
        </StackPanel>
    </StackPanel>
</UserControl>
