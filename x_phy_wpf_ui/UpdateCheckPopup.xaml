<Window x:Class="x_phy_wpf_ui.UpdateCheckPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Update Check"
        Width="380" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize">
    <Border Background="{DynamicResource Brush.Card}" CornerRadius="8" Padding="0" BorderThickness="1" BorderBrush="{DynamicResource Brush.Border}">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.25"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title bar -->
            <Border Grid.Row="0" Background="{DynamicResource Brush.Surface}" CornerRadius="8,8,0,0" Padding="16,12,16,12">
                <TextBlock x:Name="TitleText" Text="Update Check"
                           FontWeight="SemiBold" FontSize="14" Foreground="{DynamicResource Brush.TextPrimary}"/>
            </Border>

            <!-- Content: icon + message + OK button -->
            <Grid Grid.Row="1" Margin="20,16,20,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <materialDesign:PackIcon Grid.Column="0" Kind="Information" 
                                        Width="32" Height="32" 
                                        Foreground="{DynamicResource Brush.Info}"
                                        VerticalAlignment="Top" Margin="0,0,16,0"/>
                <StackPanel Grid.Column="1">
                    <TextBlock x:Name="MessageText" Text="You are running the latest version!"
                               FontSize="13" Foreground="{DynamicResource Brush.TextPrimary}"
                               TextWrapping="Wrap" Margin="0,0,0,20"/>
                    <Button x:Name="OkButton" Content="OK" HorizontalAlignment="Right"
                            MinWidth="80" Padding="20,8"
                            Background="{DynamicResource Brush.Primary}"
                            Foreground="{DynamicResource Brush.White}"
                            FontSize="13" FontWeight="SemiBold"
                            Cursor="Hand" BorderThickness="0"
                            Click="OkButton_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                    CornerRadius="6" 
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
