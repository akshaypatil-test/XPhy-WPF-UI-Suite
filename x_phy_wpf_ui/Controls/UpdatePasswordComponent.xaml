<UserControl x:Class="x_phy_wpf_ui.Controls.UpdatePasswordComponent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1000">
    <UserControl.Resources>
        <SolidColorBrush x:Key="PrimaryPink" Color="#E2156B"/>
        <SolidColorBrush x:Key="PrimaryPinkHover" Color="#F0458A"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#B0B0B0"/>
        <SolidColorBrush x:Key="InputBackground" Color="#1A1A1A"/>
        <SolidColorBrush x:Key="InputBorder" Color="#2A2A2A"/>
        <SolidColorBrush x:Key="InputBorderFocused" Color="#E2156B"/>
        <SolidColorBrush x:Key="ErrorText" Color="#FF6B6B"/>

        <Style x:Key="ModernPasswordBoxInnerStyle" TargetType="PasswordBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8,40,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center" Margin="0"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ModernTextBoxInnerStyle" TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8,40,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center" Margin="0"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="LaunchButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryPink}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="24,14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryPinkHover}"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#555555"/>
                    <Setter Property="Foreground" Value="#999999"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="Transparent">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Left" Width="400" Margin="50,40,50,50">

                <TextBlock Text="Update Your Password"
                           FontSize="24" FontWeight="Bold"
                           Foreground="{StaticResource TextPrimary}"
                           TextAlignment="Center" Margin="0,0,0,8"/>
                <TextBlock Text="For Security Reasons, You Must Change The Default Password Upon First Login."
                           FontSize="14" Foreground="{StaticResource TextSecondary}"
                           TextAlignment="Center" TextWrapping="Wrap"
                           Margin="0,0,0,32"/>

                <TextBlock x:Name="ErrorMessageText" Text=""
                           FontSize="12" Foreground="{StaticResource ErrorText}"
                           TextAlignment="Center" TextWrapping="Wrap" Margin="0,0,0,16" Visibility="Collapsed"/>

                <!-- Current Password -->
                <TextBlock Text="Current Password" FontSize="12" FontWeight="Bold"
                           Foreground="{StaticResource TextSecondary}" Margin="0,0,0,8"/>
                <Border x:Name="CurrentPasswordFieldBorder"
                        Background="{StaticResource InputBackground}"
                        BorderBrush="{StaticResource InputBorder}"
                        BorderThickness="1" CornerRadius="8" MinHeight="44" Margin="0,0,0,4">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="0">
                            <PasswordBox x:Name="CurrentPasswordBox"
                                         Style="{StaticResource ModernPasswordBoxInnerStyle}"
                                         PasswordChanged="CurrentPasswordBox_PasswordChanged"/>
                            <TextBox x:Name="CurrentPasswordReveal"
                                     Style="{StaticResource ModernTextBoxInnerStyle}"
                                     Visibility="Collapsed" TextChanged="CurrentPasswordReveal_TextChanged"/>
                            <TextBlock x:Name="CurrentPasswordPlaceholder" Text="Enter your current password"
                                       Foreground="{StaticResource TextSecondary}" FontSize="14"
                                       Margin="16,8,40,8" VerticalAlignment="Center" IsHitTestVisible="False"/>
                        </Grid>
                        <Button x:Name="CurrentPasswordEye" Grid.Column="1" Width="40" Height="36"
                                VerticalAlignment="Center" Margin="0,0,8,0"
                                Background="Transparent" BorderThickness="0" Cursor="Hand"
                                Click="CurrentPasswordEye_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" Padding="6">
                                        <Viewbox Width="18" Height="18">
                                            <Path Fill="{StaticResource TextSecondary}" Data="M12,4.5 C7,4.5 2.73,7.61 1,12 2.73,16.39 7,19.5 12,19.5 17,19.5 21.27,16.39 23,12 21.27,7.61 17,4.5 12,4.5 Z M12,17 C9.24,17 7,14.76 7,12 7,9.24 9.24,7 12,7 14.76,7 17,9.24 17,12 17,14.76 14.76,17 12,17 Z"/>
                                        </Viewbox>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>
                </Border>

                <!-- New Password (with info icon) -->
                <Grid Margin="0,16,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="New Password" FontSize="12" FontWeight="Bold"
                               Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                    <Button x:Name="NewPasswordInfoButton" Grid.Column="1" Width="20" Height="20"
                            Margin="6,0,0,0" Background="Transparent" BorderThickness="0" Cursor="Hand"
                            ToolTip="Password must be at least 8 characters and contain uppercase, lowercase, number, and special character (@$!%*?&amp;)."
                            Click="NewPasswordInfoButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="10">
                                    <TextBlock Text="i" FontSize="12" FontWeight="Bold"
                                               Foreground="{StaticResource TextSecondary}"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
                <Border x:Name="NewPasswordFieldBorder"
                        Background="{StaticResource InputBackground}"
                        BorderBrush="{StaticResource InputBorder}"
                        BorderThickness="1" CornerRadius="8" MinHeight="44" Margin="0,0,0,4">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="0">
                            <PasswordBox x:Name="NewPasswordBox"
                                         Style="{StaticResource ModernPasswordBoxInnerStyle}"
                                         PasswordChanged="NewPasswordBox_PasswordChanged"/>
                            <TextBox x:Name="NewPasswordReveal"
                                     Style="{StaticResource ModernTextBoxInnerStyle}"
                                     Visibility="Collapsed" TextChanged="NewPasswordReveal_TextChanged"/>
                            <TextBlock x:Name="NewPasswordPlaceholder" Text="Create new password"
                                       Foreground="{StaticResource TextSecondary}" FontSize="14"
                                       Margin="16,8,40,8" VerticalAlignment="Center" IsHitTestVisible="False"/>
                        </Grid>
                        <Button x:Name="NewPasswordEye" Grid.Column="1" Width="40" Height="36"
                                VerticalAlignment="Center" Margin="0,0,8,0"
                                Background="Transparent" BorderThickness="0" Cursor="Hand"
                                Click="NewPasswordEye_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" Padding="6">
                                        <Viewbox Width="18" Height="18">
                                            <Path Fill="{StaticResource TextSecondary}" Data="M12,4.5 C7,4.5 2.73,7.61 1,12 2.73,16.39 7,19.5 12,19.5 17,19.5 21.27,16.39 23,12 21.27,7.61 17,4.5 12,4.5 Z M12,17 C9.24,17 7,14.76 7,12 7,9.24 9.24,7 12,7 14.76,7 17,9.24 17,12 17,14.76 14.76,17 12,17 Z"/>
                                        </Viewbox>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>
                </Border>
                <TextBlock x:Name="NewPasswordErrorText" Text="" FontSize="11" Foreground="{StaticResource ErrorText}"
                           Margin="4,0,0,0" Visibility="Collapsed" TextWrapping="Wrap"/>

                <!-- Confirm Password -->
                <TextBlock Text="Confirm Password" FontSize="12" FontWeight="Bold"
                           Foreground="{StaticResource TextSecondary}" Margin="0,16,0,8"/>
                <Border x:Name="ConfirmPasswordFieldBorder"
                        Background="{StaticResource InputBackground}"
                        BorderBrush="{StaticResource InputBorder}"
                        BorderThickness="1" CornerRadius="8" MinHeight="44" Margin="0,0,0,4">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="0">
                            <PasswordBox x:Name="ConfirmPasswordBox"
                                         Style="{StaticResource ModernPasswordBoxInnerStyle}"
                                         PasswordChanged="ConfirmPasswordBox_PasswordChanged"/>
                            <TextBox x:Name="ConfirmPasswordReveal"
                                     Style="{StaticResource ModernTextBoxInnerStyle}"
                                     Visibility="Collapsed" TextChanged="ConfirmPasswordReveal_TextChanged"/>
                            <TextBlock x:Name="ConfirmPasswordPlaceholder" Text="Confirm your password"
                                       Foreground="{StaticResource TextSecondary}" FontSize="14"
                                       Margin="16,8,40,8" VerticalAlignment="Center" IsHitTestVisible="False"/>
                        </Grid>
                        <Button x:Name="ConfirmPasswordEye" Grid.Column="1" Width="40" Height="36"
                                VerticalAlignment="Center" Margin="0,0,8,0"
                                Background="Transparent" BorderThickness="0" Cursor="Hand"
                                Click="ConfirmPasswordEye_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" Padding="6">
                                        <Viewbox Width="18" Height="18">
                                            <Path Fill="{StaticResource TextSecondary}" Data="M12,4.5 C7,4.5 2.73,7.61 1,12 2.73,16.39 7,19.5 12,19.5 17,19.5 21.27,16.39 23,12 21.27,7.61 17,4.5 12,4.5 Z M12,17 C9.24,17 7,14.76 7,12 7,9.24 9.24,7 12,7 14.76,7 17,9.24 17,12 17,14.76 14.76,17 12,17 Z"/>
                                        </Viewbox>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>
                </Border>
                <TextBlock x:Name="ConfirmPasswordErrorText" Text="" FontSize="11" Foreground="{StaticResource ErrorText}"
                           Margin="4,0,0,0" Visibility="Collapsed" TextWrapping="Wrap"/>

                <Button x:Name="UpdatePasswordButton"
                        Content="Update Password"
                        Style="{StaticResource LaunchButtonStyle}"
                        Click="UpdatePassword_Click"
                        IsEnabled="False"
                        Margin="0,24,0,0"
                        HorizontalAlignment="Stretch"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
