<UserControl x:Class="x_phy_wpf_ui.Controls.StripePaymentComponent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <UserControl.Resources>
        <SolidColorBrush x:Key="BackgroundColor" Color="#000000"/>
        <SolidColorBrush x:Key="SurfaceColor" Color="#1A1A1F"/>
        <SolidColorBrush x:Key="PrimaryColor" Color="#E2156B"/>
        <SolidColorBrush x:Key="PrimaryHoverColor" Color="#F0458A"/>
        <SolidColorBrush x:Key="TextColor" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="SecondaryTextColor" Color="#999999"/>
        <SolidColorBrush x:Key="ErrorColor" Color="#EF4444"/>
        
        <!-- Back Button Style -->
        <Style x:Key="BackButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource SecondaryTextColor}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
                    <Setter Property="Background" Value="#2A2A2A"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource BackgroundColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Back Button -->
        <Button Grid.Row="0"
                Content="← Back to Plans"
                Style="{StaticResource BackButtonStyle}"
                Click="BackButton_Click"
                Margin="0,0,0,8"
                HorizontalAlignment="Left"/>

        <!-- Header -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="Complete Your Payment"
                          FontSize="24"
                          FontWeight="Bold"
                          Foreground="{StaticResource TextColor}"
                          HorizontalAlignment="Center"/>
                <TextBlock Text="Securely Complete Your Subscription Purchase."
                          FontSize="14"
                          Foreground="{StaticResource SecondaryTextColor}"
                          HorizontalAlignment="Center"
                          Margin="0,6,0,0"/>
                <TextBlock x:Name="PlanDetailsText"
                          FontSize="14"
                          Foreground="{StaticResource SecondaryTextColor}"
                          HorizontalAlignment="Center"
                          Margin="0,4,0,0"/>
            </StackPanel>
        </Grid>

        <!-- WebView2 Container -->
        <Border Grid.Row="2" 
                Background="White"
                CornerRadius="8"
                Margin="0,0,0,16">
            <!-- Source is set dynamically in code-behind after injecting payment details -->
            <wv2:WebView2 x:Name="StripeWebView" 
                         Source="about:blank"/>
        </Border>

        <!-- Loading/Error Messages -->
        <StackPanel Grid.Row="3">
            <TextBlock x:Name="StatusText"
                      FontSize="13"
                      Foreground="{StaticResource SecondaryTextColor}"
                      TextAlignment="Center"
                      TextWrapping="Wrap"
                      Margin="0,0,0,16"
                      Text="Loading secure payment form..."/>

            <TextBlock x:Name="ErrorMessageText"
                      FontSize="13"
                      Foreground="{StaticResource ErrorColor}"
                      TextWrapping="Wrap"
                      TextAlignment="Center"
                      Margin="0,0,0,16"
                      Visibility="Collapsed"/>

            <TextBlock FontSize="11"
                      Foreground="{StaticResource SecondaryTextColor}"
                      TextAlignment="Center"
                      TextWrapping="Wrap">
                <Run Text="🔒 Secured by Stripe"/>
                <LineBreak/>
                <Run Text="Your card details are encrypted and never stored"/>
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
