<Window x:Class="x_phy_wpf_ui.AppDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="X-PHY"
        Width="380" SizeToContent="Height" MinWidth="320" MaxWidth="480"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ResizeMode="NoResize">
    <Border Background="{DynamicResource Brush.Card}" CornerRadius="8" Padding="0" BorderThickness="1" BorderBrush="{DynamicResource Brush.Border}">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.25"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title bar -->
            <Border Grid.Row="0" Background="{DynamicResource Brush.Surface}" CornerRadius="8,8,0,0" Padding="16,12,16,12">
                <TextBlock x:Name="TitleText" Text=""
                           FontWeight="SemiBold" FontSize="14" Foreground="{DynamicResource Brush.TextPrimary}"/>
            </Border>

            <!-- Content: icon + message + buttons -->
            <Grid Grid.Row="1" Margin="20,16,20,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <materialDesign:PackIcon x:Name="IconControl" Grid.Column="0" Kind="Information"
                                        Width="32" Height="32"
                                        Foreground="{DynamicResource Brush.Info}"
                                        VerticalAlignment="Top" Margin="0,0,16,0"/>
                <StackPanel Grid.Column="1">
                    <TextBlock x:Name="MessageText" Text=""
                               FontSize="13" Foreground="{DynamicResource Brush.TextPrimary}"
                               TextWrapping="Wrap" Margin="0,0,0,20"/>
                    <!-- OK only -->
                    <StackPanel x:Name="OkButtonPanel" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="OkButton" Content="OK" MinWidth="80" Padding="20,8"
                                Background="{DynamicResource Brush.Primary}"
                                Foreground="{DynamicResource Brush.White}"
                                FontSize="13" FontWeight="SemiBold"
                                Cursor="Hand" BorderThickness="0"
                                Click="OkButton_Click">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                    <!-- Yes / No -->
                    <StackPanel x:Name="YesNoButtonPanel" Orientation="Horizontal" HorizontalAlignment="Right" Visibility="Collapsed">
                        <Button x:Name="NoButton" Content="No" MinWidth="80" Padding="20,8" Margin="0,0,10,0"
                                Background="{DynamicResource Brush.Surface}"
                                Foreground="{DynamicResource Brush.TextPrimary}"
                                BorderBrush="{DynamicResource Brush.Border}" BorderThickness="1"
                                FontSize="13" FontWeight="SemiBold"
                                Cursor="Hand" Click="NoButton_Click">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button x:Name="YesButton" Content="Yes" MinWidth="80" Padding="20,8"
                                Background="{DynamicResource Brush.Primary}"
                                Foreground="{DynamicResource Brush.White}"
                                FontSize="13" FontWeight="SemiBold"
                                Cursor="Hand" BorderThickness="0"
                                Click="YesButton_Click">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
